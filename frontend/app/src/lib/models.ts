export interface Child {
    id: string
    name: string
    family_name: string
    required_qualification: string
    street: string
    city: string
    zip_code: string
    requested_hours: number
}
